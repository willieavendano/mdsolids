{
  "name": "mdsolids-web",
  "private": false,
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "description": "An open-source, cross-platform web reimagining of MDSolids for mechanics of materials.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.5"
  }
}
